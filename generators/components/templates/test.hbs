import 'match-media-mock'

import { renderWithTheme } from 'utils/helpers/tests'

import {{pascalCase name}}, { {{pascalCase name}}Props } from '.'

const props: {{pascalCase name}}Props = {
  title: '{{pascalCase name}}'
}

describe('<{{pascalCase name}} />', () => {
  it('should render the heading', () => {
    const { debug, container } = renderWithTheme(<{{pascalCase name}} {...props} />)

    const {{lowerCase name}} = screen.getByRole('heading', { name: /{{pascalCase name}}/i })

    debug(container)

    expect({{ lowerCase name}}).toBeInTheDocument()
  })
})
